{
  "name": "soga-panel-backend",
  "version": "1.0.0",
  "description": "Lightweight proxy panel compatible with Soga v1 WebAPI",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev --port 18787 -c wrangler-dev.toml",
    "deploy": "wrangler deploy --minify",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test:email": "node test/email-verification.mjs"
  },
  "keywords": [
    "cloudflare-workers",
    "proxy",
    "vpn",
    "soga",
    "v2ray",
    "trojan"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "resend": "^6.12.0",
    "worker-mailer": "^1.2.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260420.1",
    "c8": "^11.0.0",
    "node-fetch": "^3.3.2",
    "typescript": "^6.0.3",
    "wrangler": "^4.83.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
